tosca_definitions_version: tosca_simple_yaml_1_0_0
data_types:
  onap.datatypes.monitoring.dmaap.dmaapProducerConfiguration:
    derived_from: tosca.datatypes.Root
    properties:
      dmaap producer configuration:
        type: string
        description: configuration for changeIdentifier and feedName
policy_types:
  onap.policies.Monitoring:
    derived_from: tosca.policies.Root
    description: a base policy type for all policies that govern monitoring provision
    version: 1.0.0
  onap.policy.monitoring.dcaegen2.collectors.datafile.datafile-app-server:
    derived_from: onap.policies.Monitoring
    properties:
      dmaap.dmaapProducerConfiguration:
        type: string
        description: 'datafile collector publishes data to data router feed '
        entry_schema:
          type: onap.datatypes.monitoring.dmaap.dmaapProducerConfiguration